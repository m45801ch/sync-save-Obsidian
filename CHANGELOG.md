# Changelog

## 1.2.3 - 2026-08-22

### 整理本地資料庫
- 新增「整理本地資料庫（僅本機）」同步模式，掃描本地 Vault 所有檔案並以檔名判斷重複
- 保留非 `public/` 路徑的分類檔案，只將 `public/` 及其子資料夾中的重複檔移至設定的本機回收桶
- 此模式不連線、不上傳、不下載，也不修改雲端資料

## Next

## 1.2.2 - 2026-08-22

### 同名檔案清理與同步摘要
- 雙向同步、增量拉取與增量拉取帶刪除會偵測新加入 `public/` 的檔案；若其他資料夾已有相同檔名，保留分類檔並將 `public/` 檔案移至設定的本機回收桶
- 雲端的同名 `public/` 檔案同步移至 `.sync-trash/`，避免下次同步重複下載或永久刪除
- 同步完成或被大量變更保護阻擋後，顯示同步數量、成功數量、失敗數量與丟入回收桶數量

## 1.2.1 - 2026-08-20

### 介面與同步狀態
- 大量變更保護新增 0–100、每 10% 一格的下拉選單，以及自訂門檻輸入；0 明確標示為關閉保護
- 同步開始後先顯示已規劃的動作數量，執行中顯示目前檔案進度與雲端服務名稱
- 被大量變更保護阻擋時改為明確顯示「因大量變更保護-同步未執行」，多雲端同步分別顯示各服務的結果

## 1.2.0 - 2026-08-20

### 新功能
- 新增雙向同步、增量推送、增量拉取、增量推送帶刪除與增量拉取帶刪除五種同步方向
- 新增「保留較新版本」與「保留較大版本」衝突處理，以及智慧衝突副本功能
- 新增大量變更保護、衝突副本掃描與確認刪除流程

### 修正
- 修正 Google Drive 同步可能將檔案內容覆寫為 `{}` 的問題，現在會保留並驗證完整檔案內容
- 修正雙向同步未正確比較同名檔案，並依修改時間、大小與雜湊值判斷變更
- 修正同步寫入失敗時可能清空既有檔案的問題，改用驗證後的暫存檔與原子替換
- 遠端刪除一律先移至 `.sync-trash/`；本機拉取帶刪除可選擇系統或 Obsidian 垃圾桶

### 效能
- 加密同步 manifest 分開記錄本機與遠端 payload 大小，避免每次規劃同步都重新下載共享檔案

## 1.1.0 - 2026-08-16

### 新功能
- 新增「備份並同步刪除」同步模式：本機刪除的檔案會搬移到雲端垃圾桶（`.sync-trash/`），維持本機與雲端鏡像一致
- 新增雲端垃圾桶設定：保留天數、獨立清除定時器，以及手動「立即清除垃圾桶」與指令「清除雲端垃圾桶」
- 支援多雲端依序同步：S3、WebDAV、Dropbox、OneDrive、Google Drive、Box
- 新增 Dropbox / OneDrive OAuth2 授權與帳號名稱顯示
- 新增 Google Drive PKCE 與 Box 免伺服器一鍵授權
- 重構設定介面、優化按鈕微動效與視覺對比、更換自訂圖示

### 修正
- 修正狀態列進度在同步期間未更新的問題
- 修正 `manifest.files` 未定義時可能發生的錯誤
- 修正 Box 舊版設定預設切換為「一鍵連結」授權
